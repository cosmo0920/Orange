.notice
  = notice

= link_to 'Add Book', new_book_path

%table#booklist
  - @books.each do |book|
    %tr
      %td
        = link_to image_tag(book.image_url, alt: book.image_url), edit_book_path(book),
        alt: book.title,  class: "book_image"
      %td=book.title
      %td=link_to 'Edit', edit_book_path(book.id)
= paginate @books
